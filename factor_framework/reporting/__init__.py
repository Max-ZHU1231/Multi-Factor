"""
factor_framework.reporting
===========================
Reporting layer (v4.0): run manifest generation and artifact persistence.

This module will grow to contain:
    RunManifest     — structured run metadata (run_id, config_hash, metrics, env)
    ReportWriter    — artifact serialisation (CSV / JSON / Parquet)

Phase A stub — full implementation in Phase D (manifest + cache key upgrade).
"""
