"""
factor_framework.cli
====================
CLI layer (v4.0): unified entry-point for the Multi-Factor framework.

Sub-commands (Phase B implementation):
    mf screen   — single-factor or batch screening
    mf batch    — full-batch factor validation
    mf validate — semantic / look-ahead validation suite
    mf cache    — cache management (info / clear / gc)
    mf report   — report generation from saved artifacts

Phase A stub — full implementation in Phase B (CLI convergence).
"""
